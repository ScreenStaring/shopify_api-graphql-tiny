RSpec.describe ShopifyApi::Graphql::Tiny do
  it "has a version number" do
    expect(ShopifyApi::Graphql::Tiny::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
